[package]
name = "benchmark"
version = "0.1.0"
authors = []
edition = "2018"

[lib]
name = "benchmark"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.26.0"
rustler_codegen = "0.26.0"
